# OS
.DS_Store
Thumbs.db

# IDE
.idea/
*.iml
.vscode/

# PHP / Composer
/vendor/
composer.phar

# Node (only if a build step is added later)
/node_modules/

# Logs & temp
*.log
*.tmp
/tmp/

# Local env / secrets
.env
*.local

# SDD scratch (briefs/reports/diffs/ledger)
.superpowers/
.phpunit.result.cache
.claude/
e2e/node_modules/
docs/superpowers/
bin/deploy.conf
test-results/
# Screenshots a throwaway probe drops beside the specs. One reached a commit; the same class of
# leftover once reached the WordPress.org package (test-results/.last-run.json).
e2e/*.png
e2e/probe_*.spec.js
